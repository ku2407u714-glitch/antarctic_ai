# Demo models

The trained `sea_ice_model.joblib` file is generated locally by `python ml/train_sea_ice.py`. The dashboard intentionally uses a transparent demo calculation by default so it works immediately and does not imply scientific validation. Replace this model and connect validated data before any operational use.
